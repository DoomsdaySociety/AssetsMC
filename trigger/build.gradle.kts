plugins {
    id("java")
}

repositories {
    maven("https://maven.fastmirror.net/repositories/minecraft")
    maven("https://jitpack.io")
}

dependencies {
    /*
    compileOnly(
        group = "com.github.DoomsdaySociety",
        name = "AssetsMC",
        version = "1.0-SNAPSHOT",
        ext = "zip"
    ) */
}
